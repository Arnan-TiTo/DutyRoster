# Copy this file to `.env` (optional). Docker Compose already provides defaults.

# IMPORTANT: '@' in password must be URL-encoded as %40
DATABASE_URL="postgresql://postgres:Admin%409999@localhost:5432/duty_roster?schema=public"

# Used to sign session cookie (JWT)
AUTH_SECRET="change-me-please"
AUTH_TTL_MINUTES=120

# Optional: change demo users password
SEED_DEFAULT_PASSWORD="Admin@9999"
